<Page
    x:Class="Teacher.Book.Views.Navigation.NavigationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Teacher.Book.Views.Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <muxc:NavigationView
            x:Name="BookNavigation"
            PaneDisplayMode="LeftMinimal"
                             IsSettingsVisible="False"
                             IsBackButtonVisible="Collapsed"
                             MenuItemsSource="{x:Bind ViewModel.BookPages}"
                             SelectionChanged="NavigationView_OnSelectionChanged"
            SelectedItem="{x:Bind ViewModel.SelectedPage, Mode=TwoWay}">
            <Frame x:Name="ContentFrame"/>
        </muxc:NavigationView>
    </Grid>
</Page>
