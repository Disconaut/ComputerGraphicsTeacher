<UserControl
    x:Class="Teacher.Views.Main.Controls.CardButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Teacher.Views.Main.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="247"
    d:DesignWidth="477">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="{x:Bind ImageSource}" Stretch="UniformToFill"></ImageBrush>
        </Grid.Background>
        <Grid x:Name="CaptionBackground" HorizontalAlignment="Stretch" VerticalAlignment="{x:Bind TextPositionCast}" Height="Auto" Padding="5">
            <TextBlock x:Name="Caption" Text="{x:Bind Text}" TextAlignment="{x:Bind TextAlignment}"/>
        </Grid>
    </Grid>
</UserControl>
