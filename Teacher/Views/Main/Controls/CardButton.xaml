<UserControl
    x:Class="Teacher.Views.Main.Controls.CardButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Teacher.Views.Main.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="{x:Bind ImageSource}" Stretch="UniformToFill"></ImageBrush>
        </Grid.Background>
        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0, 0.5">
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
        <TextBlock Text="{x:Bind Text}" TextAlignment="{x:Bind TextAlignment}" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
